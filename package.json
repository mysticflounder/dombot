{
  "name": "dombot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser extension: a collapsible chat panel on every page backed by your own Ollama, with tools that read and change the page and saved changes that replay on load.",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "./scripts/build.sh",
    "test": "./scripts/test.sh",
    "test:unit": "node --test \"test/*.test.js\"",
    "icons": "node scripts/make-icons.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  },
  "engines": {
    "node": ">=22"
  }
}
